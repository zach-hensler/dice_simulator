{
  "name": "dice_sim",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://zach-hensler.github.io/dice_sim",
  "scripts": {
    "dev": "vite",
    "test": "vitest --run",
    "build": "tsc && npm run test && vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "dependencies": {
    "preact": "^10.19.5",
    "vitest": "^1.3.1"
  },
  "devDependencies": {
    "@preact/preset-vite": "^2.8.1",
    "typescript": "^5.2.2",
    "vite": "^5.1.4"
  }
}
